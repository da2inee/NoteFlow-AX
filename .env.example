# 복사: cp .env.example .env
# (같은 폴더에) noteflow.env 를 써도 됩니다. 한 줄짜리 파일 onenote_client_id.txt(클라이언트 ID만)도 됩니다.
# .env 는 .gitignore 되어 커밋되지 않습니다. 파일은 onenote_publish.py 와 같은 디렉터리에 둡니다.
ONENOTE_CLIENT_ID=여기에-애플리케이션-클라이언트-id
